function maximumGap(nums: number[]): number {
    if (nums.length === 1) return 0
    nums = nums.sort((a,b)=>a-b)
    let difference = 0
    for(let i = 0; i < nums.length; i++){
        if( nums[i + 1] - nums[i] > difference) difference = nums[i + 1] - nums[i]
    }
    return difference
};
