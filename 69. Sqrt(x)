function mySqrt(x: number): number {
    if(x === 0) return 0
    let str = String(x)
    let exp = Number(1 + "0".repeat( str.length % 2 === 0 ? str.length / 2 - 1 : ( str.length - 1 ) / 2 ) )
    while( ( exp + 1 ) * ( exp + 1 ) <= x){
        exp++
    }
    return exp
};
