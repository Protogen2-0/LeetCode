function plusOne(digits: number[]): number[] {
    for(let i = digits.length - 1; i > -1; i--){
        if(digits[i] + 1 < 10) {
            digits[i] += 1
            return digits
        }
        digits[i] = 0
    }
    return [1, ...digits]
};
